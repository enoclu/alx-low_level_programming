#include "main.h"
/**
 * main - the function is to loop the alphabet character
 *
 * print_alphabet - this is where the coe for this ans found.
 *
 *print_alphabet - for all codes
 * Return: 0
 */

void print_alphabet(void)
{
	char alp;

	for (alp = 'a'; alp <= 'z'; alp++)
		_putchar(alp);

	_putchar("\n");

	return (0);
}
